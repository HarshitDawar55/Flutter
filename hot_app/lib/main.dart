import 'package:flutter/material.dart';

import 'home.dart';

void main() {
  runApp(HotApp());
}

class HotApp extends StatelessWidget {
  // ignore: non_constant_identifier_names
  build(BuildContext HotAppContext) {
    return MyApp();
  }
}
